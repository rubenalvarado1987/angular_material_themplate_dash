import { TextColorDirective } from './text-color.directive';

describe('TextColorDirective', () => {
  it('should create an instance', () => {
    const directive = new TextColorDirective(null,null);
    expect(directive).toBeTruthy();
  });
});
